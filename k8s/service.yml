apiVersion: v1
kind: Service
metadata:
  name: team-service
  namespace: $(kubernetes-namespace)
  annotations:
    prometheus.io/scrape: 'true'
  labels:
    app: team-service
    project: selfservice
    department: devex    
spec:
  ports:
  - name: external
    port: 80
    targetPort: web
  selector:
    app: team-service